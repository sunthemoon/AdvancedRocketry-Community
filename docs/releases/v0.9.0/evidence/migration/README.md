# v0.9.0 candidate migration evidence

This packaged-server run is bound to tested implementation commit
`f6cd77cebdb0a851cab76accbf66de565473b545` and candidate JAR SHA-256
`fbddf66938000cba369a83d4a22ff36b5ff1c9c635a0abd14f672b454e3946ad`.

The harness seeded all five hash-inventoried schema-1 roots into a copy of the
accepted dedicated-server smoke world. On first start the packaged mod:

1. decoded and semantically validated all five sources;
2. created a byte-exact backup and SHA-256 manifest before replacement;
3. staged, reread, and committed five schema-2 files;
4. reported `ARCE-BETA-1002` and all five services operational;
5. stopped cleanly, restarted the same world, reported `ARCE-BETA-1001`, and
   again reported every service operational.

`summary.json` binds the commit, artifact, fixtures, before/after hashes,
backup, Java runtime, and both server cycles. `backup-manifest.json` is the
runtime-generated byte-exact source inventory. `filtered-lifecycle.log`
retains only stable migration and operator-report lines. Machine-local complete
logs are omitted because they can contain installation paths.
