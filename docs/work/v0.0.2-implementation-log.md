# v0.0.2 Implementation Log

```yaml
version: v0.0.2
status: PASSED
started_at: 2026-08-26
last_updated: 2026-08-30
working_branch: codex/v0.0.2-forge-bootstrap
base_commit: 86b9db01b1cb4c8b8f673590baf1dc185d1716b3
tested_implementation_commit: d6c8464b0e75fe10d64fcb579ab08345f7d4cd3b
documentation_checkpoint: db9ce96113712dd93e8db05736b3a9ed764e41a8
acceptance_evidence_source_commit: cf476b9601fc482977d1716617c87e4b2cbf704f
build: 1.20.1-0.0.2-dev
```

## Continuation task register

- [x] **V002-CONT-001 — Forge MDK and Gradle Wrapper provenance review**
  - Status: `VERIFIED`; owner approval, rebuild, exact-Git final review, and
    rendered README review are complete.
  - 2026-08-30 audit correction: an independent primary-source review found
    that the adapted MDK targets did not yet carry the dated modification
    notices required by the recorded LGPL 2.1 treatment. Commit `67929aa`
    adds an in-file license/provenance/modifier/date notice to all eight
    retained MDK targets. The pending provenance record now also requires the
    companion sources JAR and exact repository revision to be offered with the
    binary artifact. This correction invalidates the earlier review packet and
    pre-correction JAR hashes; neither may be approved or used for client Gate
    evidence.
  - Observable outcome: exact official inputs, commits, member/target hashes,
    licenses, transformations, and affected paths are recorded in both human-
    and machine-readable form; the exact supplemental license copies and notice
    are packaged under `META-INF`.
  - Verification: both copied licenses match their official source hashes; the
    repository and JAR validators reject changed copies.
  - Evidence:
    [`../provenance/v0.0.2-forge-mdk-and-gradle-wrapper.md`](../provenance/v0.0.2-forge-mdk-and-gradle-wrapper.md),
    [`../../THIRD-PARTY-NOTICES.md`](../../THIRD-PARTY-NOTICES.md).
  - Approval evidence: the bootstrap record is digest-bound
    `THIRD_PARTY_APPROVED`; implementation `d6c8464` rebuilt to main JAR
    `cd5ae579...` and sources JAR `f958f433...`; exact report `7b8aef7c...`
    covers 18 inventory files, 27 commits, and 319 path changes; the 1440x5000
    README screenshot and source/resource report were approved without
    findings; the renewed source review is recorded in `db9ce96`.

- [x] **V002-CONT-002 — Artifact and CI evidence hardening**
  - Status: `VERIFIED` at
    `d6c8464b0e75fe10d64fcb579ab08345f7d4cd3b`; all three exact-head PR checks
    passed in Forge run 33302877815 and governance run 33302877802. Earlier
    documentation checkpoint `da67cfa` also had 3/3 successful checks.
  - Observable outcome: the JAR validator emits a stable per-entry content
    manifest; release checksums cover every committed evidence file and the
    external JAR; DataGen cleanliness includes untracked files.
  - Verification: the historical `da67cfa` checkpoint passed 396 Python tests
    and the earlier 15-check repository contract. The current checkpoint passes
    519 Python tests; final strict validation reports 18 PASS, 0 PENDING, 0 WARN,
    and 0 FAIL. The current 34-entry JAR and complete evidence checksum inventory
    validate.
  - Evidence:
    [`../releases/v0.0.2/evidence/artifact/jar-content-manifest.json`](../releases/v0.0.2/evidence/artifact/jar-content-manifest.json),
    [`../releases/v0.0.2/checksums.txt`](../releases/v0.0.2/checksums.txt).

- [x] **V002-CONT-003 — Matching packaged-client acceptance**
  - Status: `VERIFIED_AND_ACCEPTED`.
  - Observable outcome required: an isolated Forge 47.4.10 client loads the
    identical JAR used by the retained loopback server, shows correct metadata,
    enters a disposable world, joins, disconnects, and reconnects after the
    same server world restarts; missing-project-mod behavior is observed.
  - Verification: three-way JAR SHA-256 equality, reviewed screenshots,
    selected redacted logs, first join/reconnect, and the actual missing-mod
    result are bound in the strict canonical schema-5 bundle.
  - Evidence: [`../releases/v0.0.2/evidence/client/`](../releases/v0.0.2/evidence/client/).

- [x] **V002-CONT-004 — G9 release audit and human acceptance**
  - Status: `VERIFIED_AND_ACCEPTED`.
  - Observable outcome: changelog, installation/dependency and save-
    compatibility guidance, known issues, checksums, Gate evidence, and human
    decisions are consistent.
  - Verification: local documentation/checksum automation, tested-
    implementation CI, client evidence integration, and the last observed
    documentation-checkpoint CI pass; owner `sunthemoon` approved G8/G9 on
    2026-08-30.

- [x] **V002-CONT-005 — Manual-evidence integrity hardening**
  - Status: `VERIFIED` as the historical baseline; the schema-4 client bundle
    binds a clean source commit,
    three physical JAR copies, two distinct non-nested client profiles, four
    canonical mod-inventory snapshots, profile-local raw logs, source README/
    manifest Git blobs, same-player join/leave cycles, warning dispositions,
    and the separate mismatch startup without archiving player secrets. The
    harness-owned player summary remains schema 3.
  - Verification: 119/119 focused collector tests cover exact matching-JAR and
    empty missing-mod inventories, profile/log reuse attacks, credential
    redaction, tampering, and atomic failure. The complete-suite result is
    recorded in
    [`../releases/v0.0.2/TEST-REPORT.md`](../releases/v0.0.2/TEST-REPORT.md).
    Strict collection validates build inputs before atomic publication and
    leaves no canonical bundle on failure.
  - Acceptance: the canonical bundle is strictly valid, all screenshots have a
    completed privacy review, and its fixed observations are `PASS`.

- [x] **V002-CONT-006 — G0 approval-state integrity hardening**
  - Status: `VERIFIED_AND_APPROVED`; schema 3 binds exact
    Git blob bytes, OIDs, object types, modes, import-parent changes, current
    HEAD state, exact notice bytes, and one approval digest domain.
  - Verification: 89/89 focused provenance tests separate mechanical `PASS`
    from human `PENDING`, reject ambiguous digest use, and require a valid
    digest-bound approved state after review. A deterministic Git-object packet
    generator/verifier has 35 focused tests and exposes the pending digest only
    as `PENDING_CONTENT_DIAGNOSTIC_ONLY`; governance CI uploads that packet
    without changing tracked files.
  - Acceptance: repository owner `sunthemoon` approved the exact scope and
    notice treatment; the validator-generated content digest is bound in the
    `THIRD_PARTY_APPROVED` record.

- [x] **V002-CONT-007 — Final-G0 review-input automation**
  - Status: `VERIFIED_AND_APPROVED`; an isolated,
    create-once generator/verifier reconstructs a bounded canonical report from
    one exact Git commit rather than mutable worktree source/resource bytes.
  - Observable outcome: the report binds the fixed v0.0.1 base, selected
    commit/tree, complete Java/resource/legal-file inventory, declared bootstrap
    coverage, main/sources JAR manifests, and relevant commit/path-change
    history. It explicitly records `INPUTS_ONLY` and makes no Gate decision.
  - Verification: 16/16 focused tests cover deterministic generation and
    verification, isolated subprocess execution, dirty worktrees, wrong or
    non-ancestor commits, graft/shallow history, bounded parent edges and Git
    output, runtime-tool binding, create-once publication, and link/reparse/
    hardlink/traversal rejection. Governance CI requires exact generation,
    verification, and a commit-named artifact for the PR head or pushed commit.
  - Acceptance: the rendered README at `d6c8464` and renewed source report at
    `3b5a261` were approved without findings and archived under canonical paths.

- [x] **V002-CONT-008 — Acceptance binding and status hardening**
  - Status: `VERIFIED_AND_ACCEPTED`; artifact implementation is `d6c8464`, the
    renewed final G0 record is `db9ce96`, and client evidence binds `cf476b9`.
  - Observable outcome: workflows check out the exact review commit; the
    schema-5 client bundle binds schema-4 manual-player summaries and schema-2
    mismatch receipt/properties records; final-G0 and G4 validators reject
    inconsistent review or Gate claims; bounded Git, Markdown, JAR, network,
    properties, log, and filesystem inputs fail closed.
  - Verification: 519/519 Python tests, 3/3 forced JUnit tests, 1/1 GameTest,
    DataGen cleanliness, 34-entry JAR audits, and both packaged-server cycles
    pass locally. Exact-head CI is 3/3 PASS. Final strict repository validation
    is 18 PASS / 0 PENDING / 0 WARN / 0 FAIL.
  - Evidence: Forge artifact 9729573591 reproduces the Windows main JAR,
    sources JAR, and content-manifest hashes. Governance artifacts 9729539499
    and 9729539691 reproduce packet manifest `b7df039f...d3b` and schema-2
    final-G0 input report `7b8aef7c...9623`.
  - Acceptance: ADR-005, G8, and G9 were approved by `sunthemoon` on 2026-08-30.

## Completed scope

- Established the Java 17 / Minecraft 1.20.1 / Forge 47.4.10 project with a
  Forge 47.4.23 advisory compatibility lane.
- Added the approved mod entrypoint, common configuration, empty registry
  boundary, explicit client/server bootstrap packages, processed metadata,
  original geometric bootstrap logo, DataGen provider, and one GameTest.
- Added JUnit, repository, artifact, side-boundary, release-checksum, clean
  worktree, status-protocol, installer-recovery, and packaged-server tests.
- Added a pinned disposable packaged-server harness. It verifies Minecraft
  version, protocol 763, exact mod marker, copied-JAR hash, save/stop, and a
  same-world restart without project warnings or client-class linkage findings;
  schema 2 binds cycle IDs, full-log hashes, exit codes, world identity, and the
  harness-owned canonical startup-properties snapshot.
- Added a stable 34-entry JAR content manifest and checksums for every committed
  v0.0.2 evidence file plus the external distributable.
- Added exact Forge/Gradle license copies, a target-level provenance record,
  a machine-readable input manifest, deterministic mechanical G0 evidence,
  third-party notices, and JAR packaging for those materials.
- Added a blocked-by-default schema-5 packaged-client evidence collector and
  validator with commit/JAR identity, schema-4 manual-player summaries,
  distinct profile inventories and logs,
  PNG structure/size, bounded credential redaction, same-player lifecycle,
  mismatch-attempt, warning-disposition, and five scoped applicability-decision
  checks, plus the strictly validated packaged-client evidence bundle.
- Added installation, changelog, and packaged-client handoff documentation
  without presenting the milestone as released or playable.
- Added deterministic final-G0 source/resource review-input generation and CI
  upload without pre-filling or inferring a human decision.

## Explicit non-goals preserved

- No original Advanced Rocketry Java source, texture, model, sound, machine,
  planet, or rocket implementation was imported.
- No JEI, Curios, Patchouli, LibVulpes replacement, gameplay, persistence,
  project network protocol, release tag, or public binary was introduced. Any
  later GitHub Release must be classified as a pre-release, never stable.
- No visible client evidence was taken from an existing personal launcher or
  game session, and no user-owned Minecraft process was controlled.
- No version `PASSED`, merge, tag, or release approval is claimed.

## Input provenance

```yaml
forge_mdk: forge-1.20.1-47.4.10-mdk.zip
forge_mdk_sha256: 73e0122becd05e39b47eced54e030380d66411850ed86786a2d58ecd886b0451
forge_mdk_sha1: 31133abd261aa4d23672d1820db06ccec80326ad
gradle_wrapper_jar_sha256: ed2c26eba7cfb93cc2b7785d05e534f07b5b48b5e7fc941921cd098628abca58
gradle_distribution_sha256: a4b4158601f8636cdeeab09bd76afb640030bb5b144aafe261a5e8af027dc612
forge_license_copy_sha256: 481c96d94d182382c4225d5b210f8c658c85350cf548f25c9f56c058804f1e57
gradle_license_copy_sha256: e5bfcf1132c8e12c3fce87d4dfbcb543cfb7202d8fa28ba85c07132e30836437
bootstrap_logo_sha256: c5c6fbc63113a51da1ec28ef1227b358b41030b09cae4103f160f37d3a343690
upstream_ar_code_or_assets_imported: false
provenance_review_status: THIRD_PARTY_APPROVED
```

## Verified behavior at the tested implementation

- Gradle 8.8 runs on Microsoft Java 17.0.8.
- Repeated clean builds produced the same current main JAR SHA-256:
  `cd5ae579bae1bc21c1f67df2c3e00f196e0ee4a9ead01653c926b88ca37f32ad`.
- The current sources JAR SHA-256 is
  `f958f4334e8f95062a6ed15257fb9c5d940759490f3dc335c70e2764f1acacbe`.
- Tested-implementation Linux CI produced the same main JAR, sources JAR, and
  content manifest bytes as Windows; their SHA-256 values are `cd5ae579...`,
  `f958f433...`, and `1384c9c4...` respectively.
- Three JUnit tests and 353 Python tests pass. The JUnit task was also forced to
  execute with `--rerun-tasks`; no result came only from the Gradle build cache.
- The current evidence-hardening checkpoint expands the Python suite to 396;
  all 396 pass locally in 1797.580 seconds while the tested implementation and
  packaged JAR bytes remain unchanged.
- Acceptance-binding checkpoint `7441cd2` expands the suite to 517; all 517 pass
  locally in 1832.229 seconds and in governance CI in 160.389 seconds.
- Structured-log checkpoint `db9ce96` expands the suite to 519; all 519 pass in
  governance run 33308011373.
- The 34-entry distributable passes identity, notice/license byte equality,
  archive path, placeholder, and credential name/content checks.
- Thirty-five checksum entries validate 34 committed evidence files and the built JAR.
- DataGen passes, leaves no generated-resource diff, and the full committed
  worktree check reports no tracked, staged, untracked, or non-ignored output.
- The side scan finds no `net.minecraft.client` reference outside the client
  package. The earlier validator reported 15/15 checks; final strict validation
  reports 18 PASS with no pending, warning, or failed state.
- Forge GameTest reports `All 1 required tests passed :)` and stops normally.
- The current tested packaged-server baseline passes two cycles with Java
  17.0.8, Forge 47.4.10,
  protocol 763, the exact mod marker, zero players, save, clean stop, and the
  same schema-2 world identity and canonical startup-properties snapshot on
  restart.
- Pull request #3 reports 3/3 successful tested-implementation checks: Forge
  run 33258532863 and governance run 33258532838. Hardened documentation
  checkpoint `da67cfa` also had 3/3 successful last-observed checks in Forge run
  33285098959 and governance run 33285099023. Governance uploaded packet
  artifact 9724184181 and final-G0 input-report artifact 9724184322; both were
  byte-identical to local formal outputs. Forge artifact 9724241324 repeated the
  main JAR, sources JAR, and content-manifest hashes above.
- Exact-head checkpoint `7441cd2` also reports 3/3 successful checks in Forge
  run 33293732867 and governance run 33293732862. Forge artifact 9726838947 and
  governance artifacts 9726778456/9726778602 match the locally verified JAR,
  packet-manifest, and final-G0 report hashes.
- The earlier `da67cfa` checkpoint packet binds 35 exact-Git inputs plus generated instructions
  (36 payloads and 37 total files). The exact-Git final report covers 18
  distributable source/resource/legal files, 11 bootstrap targets, 20 commits,
  and 38 path changes. Both are mechanical review inputs and record no approval.
- The `7441cd2` packet retains the 35 bound inputs plus generated instructions;
  its schema-2 final report covers 18 inventory files, 11 bootstrap targets, 22
  commits, and 286 path changes. It remains `INPUTS_ONLY` because the bootstrap
  provenance prerequisite is pending human review.
- Post-approval implementation `d6c8464` reports 3/3 successful checks in Forge
  run 33302877815 and governance run 33302877802. Artifact 9729573591 matches
  the Windows JARs and content manifest byte-for-byte; report artifact
  9729539691 covers 18 inventory files, 27 commits, and 319 path changes. G0
  record commit `3d82740` binds the owner-approved report and README screenshot.

## Failures and recovery retained

- The first continuation `runData` attempt failed while downloading
  `minecraft/sounds/mob/panda/worried/worried4.ogg` from Mojang. The retry
  downloaded the asset and passed in 33s; the generated resources remained
  unchanged.
- The first final Forge installer invocation exceeded its 600-second timeout
  while downloading libraries. The harness now records timeout output, retries
  with validated partial downloads, and permits an explicit installer-only
  resume while refusing directories containing server runtime state. The
  resumed final run passed.
- Historical implementation failures and their fixes remain documented in
  [`../releases/v0.0.2/TEST-REPORT.md`](../releases/v0.0.2/TEST-REPORT.md).
- One current clean-build wrapper attempt kept its own diagnostic log open below
  `build/`, so Gradle correctly failed to delete that directory. Moving only the
  wrapper log to the system temporary directory made the unchanged clean build
  pass; this was a verification-command placement error, not a source change.
- The first acceptance-hardening clean-build invocation inherited IBM Java 8
  from the shell and ForgeGradle stopped before compilation. Setting
  `JAVA_HOME` explicitly to Microsoft Java 17.0.8 made the unchanged build pass.
- A verified-empty, untracked `src/main/java/example/` directory initially added
  an unwanted sources-JAR entry. Removing only that empty directory and
  rebuilding `sourcesJar` restored the expected `f958f433...` hash.

## Post-acceptance repository action

- Merge PR #3 with a normal merge commit so all audited commits remain in
  history. Do not squash or rebase the accepted evidence bindings.
- Begin v0.1.0 only from a new version branch after the merge.
