# v0.5.0 Implementation Log

```yaml
version: v0.5.0
status: READY_FOR_AUDIT
started_at: 2026-09-01
last_updated: 2026-09-01
working_branch: codex/v0.5.0-rocket-snapshot
base_commit: 9c270e29673b97fc78dc73024c58d623b5869c48
tested_implementation_commit: "eae8d9224c708924930b781d7332eb69b6a4bf8d"
build: 1.20.1-0.5.0-dev
upstream_commit: c5cd5af62fc07cd4e0d24f06a16033f181c47c04
```

## Goal

Deliver a bounded, server-authoritative vertical slice that scans a connected
block rocket from an assembler, reports deterministic structure statistics,
converts it transactionally into a same-dimension `RocketEntity`, and restores
the exact approved structure and container contents at its original origin.

## Scope and non-goals

In scope: loaded-chunk-only connected scanning, fixed structure/NBT limits,
diagnostic validation, canonical schema-1 snapshots, content hashes, server-
recomputed statistics, region locking, fault-injected assembly rollback,
collision-safe disassembly, vanilla container adapters, a thin persistent
entity, bounded tracking-player visual synchronization, cached block rendering,
DataGen, GameTests, dedicated-server restart, concurrency, conservation, and
manual rendering evidence.

Explicit non-goals: no launch, fuel consumption, flight state machine,
destination selection, cross-dimension transfer, orbital physics, arbitrary
third-party BlockEntity movement, chunk forcing, dynamic dimensions, stations,
satellites, or v0.6+ travel behavior.

No upstream code or binary asset will be copied for this slice. Upstream is a
behavior/algorithm reference only; all Java, data, text, and visual resources
introduced here are community-authored and must be recorded as such.

## Fixed safety and authority decisions

The repository-approved defaults in `docs/03-TARGET-ARCHITECTURE.md` are the
v0.5 release limits. Reducing or increasing them requires benchmark evidence
and an ADR.

| Boundary | Value | Failure behavior |
|---|---:|---|
| Rocket blocks | 2,048 | stop scan; `TOO_MANY_BLOCKS` |
| Bounding-box volume | 32,768 | stop scan; `BOUNDING_VOLUME_EXCEEDED` |
| BlockEntities | 128 | reject before extraction |
| One approved BlockEntity NBT | 262,144 bytes | reject before extraction |
| Total snapshot NBT | 1,048,576 bytes | reject before extraction |
| Visual snapshot payload | 524,288 bytes | do not track/render; retain server entity |
| S2C visual chunk | 32,768 bytes | reject malformed count/index/length |
| Concurrent visual reassemblies/client | 8 | evict incomplete oldest entry |
| World observations per scanner tick | 256 | retain resumable task state |
| Total world observations per scan | 12,289 | `SCAN_BUDGET_EXCEEDED`; no further reads |

Only already-loaded chunks may be inspected or modified. The client submits an
operation intent and target identity, never blocks, statistics, snapshot NBT,
or material counts. Ordinary approved states may move. Vanilla chest/barrel
contents use a bounded explicit adapter. Project-owned adapters must opt in.
Every other BlockEntity, including all unregistered third-party types, is
rejected by default with its position and registry identifier. Command blocks,
portals, moving pistons, structure blocks, jigsaws, spawners, and known chunk-
loading/administrative blocks are forbidden.

## Feature map

- [x] `V050-PREP` Version boundary and safety policy - status: verified
  - [x] `V050-PREP-001` Verify merged v0.4.0 G0-G9 evidence and clean `main` base - status: verified
  - [x] `V050-PREP-002` Bind v0.5 to repository-approved limits and default-deny BE policy - status: verified
- [x] `V050-MODEL` Pure snapshot, validation, and statistics domain - status: verified
  - [x] `V050-MODEL-001` Define bounded positions, palette entries, approved BE payloads, and schema-1 snapshot - status: verified
  - [x] `V050-MODEL-002` Encode/decode canonical NBT and verify stable SHA-256 content identity - status: verified
  - [x] `V050-MODEL-003` Recompute mass, thrust, tank capacity, seats, engines, and guidance on the server - status: verified
  - [x] `V050-MODEL-004` Return stable diagnostic codes, locations, parameters, and localization keys - status: verified
- [x] `V050-SCAN` Bounded loaded-world scanner - status: verified
  - [x] `V050-SCAN-001` Scan only connected tagged states from a server-selected seed - status: verified
  - [x] `V050-SCAN-002` Reject unloaded chunks without adding tickets or loading them - status: verified
  - [x] `V050-SCAN-003` Enforce block, extent, volume, palette, BE-count, and NBT limits during growth - status: verified
  - [x] `V050-SCAN-004` Capture only approved container/project BE data - status: verified
- [x] `V050-TXN` Assembly and disassembly transactions - status: verified
  - [x] `V050-TXN-001` Lock overlapping regions and make replay/stale requests fail safely - status: verified
  - [x] `V050-TXN-002` Implement validate-snapshot-lock-extract-spawn-commit assembly phases - status: verified
  - [x] `V050-TXN-003` Roll back after snapshot, during extraction, spawn failure, and before commit - status: verified
  - [x] `V050-TXN-004` Restore exact states/approved NBT without drops or duplicate entities - status: verified
  - [x] `V050-TXN-005` Reject occupied/unloaded disassembly destinations without overwriting blocks - status: verified
- [x] `V050-FORGE` Forge blocks, entity, persistence, and operations - status: verified
  - [x] `V050-FORGE-001` Register assembler, motor, tank, seat, guidance block, and RocketEntity - status: verified
  - [x] `V050-FORGE-002` Adapt ServerLevel state/container access without client imports or chunk loads - status: verified
  - [x] `V050-FORGE-003` Persist bounded entity snapshot/stats/origin and refuse future schemas - status: verified
  - [x] `V050-FORGE-004` Expose server-side validate/assemble/disassemble/inspect operations with distance and state checks - status: verified
  - [x] `V050-FORGE-005` Recover/clear transient locks and partial operations after restart - status: verified
- [x] `V050-CLIENT` Bounded visual synchronization and diagnostics - status: verified
  - [x] `V050-CLIENT-001` Chunk a BE-free visual snapshot only to tracking players - status: verified
  - [x] `V050-CLIENT-002` Bound, hash-check, and expire client reassembly state - status: verified
  - [x] `V050-CLIENT-003` Render cached structure blocks without rebuilding the cache every frame - status: verified
  - [x] `V050-CLIENT-004` Show mass/thrust/capacity/seats and readable failure details - status: verified
- [x] `V050-DATA` Tags, recipes, models, localization, and provenance - status: verified
  - [x] `V050-DATA-001` Generate v0.5-isolated authored resources and GameTest structures - status: verified
  - [x] `V050-DATA-002` Record an exact generated-resource inventory and community-authored provenance - status: verified
- [x] `V050-VERIFY` Required Gates and release evidence - status: verified
  - [x] `V050-VERIFY-001` Unit-test every diagnostic, schema/size bound, stats case, rollback phase, and replay case - status: verified
  - [x] `V050-VERIFY-002` GameTest legal/illegal structures, chest conservation, occupied disassembly, and two-request contention - status: verified
  - [x] `V050-VERIFY-003` Execute 100 assembly/disassembly cycles with exact material/inventory conservation - status: verified
  - [x] `V050-VERIFY-004` Run packaged dedicated-server start/restart/rejoin and stale-operation recovery - status: verified
  - [x] `V050-VERIFY-005` Benchmark the maximum structure and archive scan/sync/render measurements - status: verified
  - [x] `V050-VERIFY-006` Capture manual diagnostics/rendering evidence and prepare G0-G9 audit bundle - status: verified
  - [x] `V050-VERIFY-007` Obtain owner Gate review, pass CI, merge, and reproduce the accepted JAR - status: verified

## Active slice

### `V050-VERIFY-007` Pull-request acceptance and post-merge reproduction

- Status: `verified`
- Observable outcome: all three pull-request checks pass, the accepted candidate
  merges, and a clean rebuild reproduces the bound main JAR SHA-256.
- Scope / non-goals: CI, review metadata, merge, and reproducibility only; no
  v0.6 flight behavior may enter this branch.
- Dependencies / interfaces: complete local G0-G9 bundle and owner approval.
- Acceptance criteria: 3/3 checks pass, the accepted Gate snapshot records the
  PR/CI URLs, `main` contains the merge, and the post-merge build hash is
  `45782780eeec54f1710cee4425f96b4d0152d29590559f519130ca9f227f0ba0`.
- Verification: GitHub checks plus local clean post-merge build and artifact
  audit.
- Evidence: [`../releases/v0.5.0/`](../releases/v0.5.0/),
  [PR #9](https://github.com/sunthemoon/AdvancedRocketry-Community/pull/9),
  [Forge CI](https://github.com/sunthemoon/AdvancedRocketry-Community/actions/runs/33451413796),
  and [governance CI](https://github.com/sunthemoon/AdvancedRocketry-Community/actions/runs/33451413748).

## Decision log

- `2026-09-01`: v0.4.0 is accepted and merged at
  `9c270e29673b97fc78dc73024c58d623b5869c48`; v0.5 starts from that exact
  clean commit on its own branch.
- `2026-09-01`: adopt the existing target-architecture limits unchanged.
- `2026-09-01`: default-deny every BlockEntity except an explicit bounded
  adapter; initially support vanilla chest/barrel contents and project-owned
  types only.
- `2026-09-01`: v0.5 entities remain in their source dimension and restore only
  at the captured origin. All fuel, launch, destination, and transfer behavior
  remains reserved for v0.6.

## Verification log

- `2026-09-01`: local `main` and `origin/main` both resolve to
  `9c270e29673b97fc78dc73024c58d623b5869c48` with a clean worktree. The
  accepted v0.4 Gate record lists G0-G9 `PASS`, and its post-merge Forge and
  governance workflows passed before this branch was created.
- `2026-09-01`: 29/29 focused JUnit tests verify immutable/canonical snapshot
  ordering, defensive NBT, long-safe bounds, all initial count/size limits,
  server-resolved statistics, stable diagnostics, schema-1 expanded/compressed
  round trips, deterministic gzip bytes, future-schema preservation, malformed
  type rejection, SHA-256 tamper detection, and compressed expansion limits.
  The complete Java suite passes 115/115 with no failures, errors, or skips.
- `2026-09-01`: 11/11 scanner tests verify connected-component isolation,
  exact per-step budgets, single observation per position, loaded-only failure,
  absolute diagnostics, component validation, block/palette/volume/BE/total-NBT
  limits, coordinate overflow, inconsistent observations, and terminal
  idempotence.
- `2026-09-01`: 14/14 transaction tests verify all four required assembly
  failure points, spawn/extraction failures, explicit rollback-failure retention,
  occupied/unloaded disassembly, partial-disassembly rollback, entity-removal
  failure, overlap locks, replay/ledger bounds, journal write/cleanup failures,
  and 100 consecutive exact block/container assembly-disassembly cycles.
  With the scanner and transaction layers present, the complete Java suite
  passes 140/140 with no failures, errors, or skips.
- `2026-09-01`: final local verification passes 156/156 JUnit tests and 609
  Python tests (606 passed, three historical-artifact skips), audits the
  497-entry JAR, reproduces the original 703,103-byte main JAR and 356,986-byte
  sources JAR, generates 39 DataGen files with zero writes, and passes strict
  repository validation with 33 passes, zero warnings, and zero failures.
- `2026-09-01`: the first final GameTest attempt failed the inherited
  `doorChangeInvalidatesAndRecovers` test because its same-tick cross-dimension
  scan observed transient `seedSky=true` despite an iron roof. No assertion or
  timeout was changed; the unchanged rerun passed all 34 required tests. The
  transient is disclosed in v0.5 known issues, and CI recurrence blocks merge.
- `2026-09-01`: PR #9 reproduced the same closed-door failure on a clean Linux
  GameTest world. The test now waits one normal server tick for the fresh Moon
  chunk heightmap, without changing production atmosphere decisions, timeout,
  or assertions. A clean local run passed 34/34, and two clean rebuilds produced
  the same 703,307-byte candidate JAR with SHA-256
  `45782780eeec54f1710cee4425f96b4d0152d29590559f519130ca9f227f0ba0`.
- `2026-09-01`: packaged-server evidence binds exact entity NBT across restart,
  four-block and container recovery after a staged pre-commit journal, and
  clean journal removal. A matching Forge user-development client joined the
  packaged server after restart and rendered the four-block cached entity.
- `2026-09-01`: PR #9 passed 3/3 checks and merged as
  `90587983b78920ed1f62621c11825dfc11dd901b`. A post-merge
  `clean build --no-build-cache --rerun-tasks` executed all 12 Gradle tasks,
  passed 156/156 JUnit tests, and reproduced the accepted 703,307-byte main JAR,
  357,173-byte sources JAR, and 497-entry content manifest exactly. G0-G9 are
  accepted and v0.5.0 is `PASSED`.
