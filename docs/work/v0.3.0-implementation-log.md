# v0.3.0 Implementation Log

```yaml
version: v0.3.0
status: IN_PROGRESS
started_at: 2026-08-31
last_updated: 2026-08-31
working_branch: codex/v0.3.0-celestial-codec
base_commit: 11d0ba48b98c3a520948e83988a46a713c9fa08d
tested_implementation_commit: ""
build: 1.20.1-0.3.0-dev
upstream_commit: c5cd5af62fc07cd4e0d24f06a16033f181c47c04
```

## Goal

Deliver an Earth/Moon/Space celestial slice with bounded data definitions,
stable `ResourceKey<Level>` identities, fixed Moon and Space worlds,
schema-versioned global state, safe operator commands, a finite client display
snapshot, and a secure import-only legacy XML adapter.

## Scope and non-goals

In scope: three definitions, parent/orbit/atmosphere validation, immutable
reload catalog, Overworld `SavedData`, basic gravity/vacuum profile services,
fixed Level JSON, bounded safe-platform teleport, diagnostic commands, S2C
display snapshot, XML DTO/parser/export/report, DataGen, unit/GameTests,
packaged-server restart, two-player consistency, and visible client evidence.

Explicit non-goals: runtime Level registration, random planets, rocket travel,
space stations, atmosphere damage, life support, custom sky rendering, legacy
world loading, or support for every legacy XML tag.

## Feature map

- [x] `V030-DECISION` Fixed-dimension and reload policy - status: verified
  - [x] `V030-DECISION-001` Accept fixed Moon/Space ADR and reject runtime dynamic Levels - status: verified
- [x] `V030-MODEL` Bounded celestial definition catalog - status: verified
  - [x] `V030-MODEL-001` Define atmosphere, orbit, and body Codecs with hard bounds - status: verified
  - [x] `V030-MODEL-002` Validate IDs, parents, cycles, fixed mappings, and catalog size - status: verified
  - [x] `V030-MODEL-003` Load an immutable all-or-nothing datapack catalog - status: verified
- [ ] `V030-STATE` World state and environment services - status: in_progress
  - [ ] `V030-STATE-001` Persist discovery and first visit in schema-1 Overworld SavedData - status: implemented
  - [x] `V030-STATE-002` Preserve unsupported future schema and reject mutation - status: verified
  - [ ] `V030-STATE-003` Resolve gravity and vacuum profiles without per-tick catalog rebuild - status: implemented
- [ ] `V030-INTEGRATION` Fixed worlds, commands, and client snapshot - status: in_progress
  - [x] `V030-INTEGRATION-001` Generate stable Earth/Moon/Space definitions and Moon/Space Level data - status: verified
  - [x] `V030-INTEGRATION-002` Add bounded `validate`, `list`, and operator-only `goto` commands - status: verified
  - [x] `V030-INTEGRATION-003` Create bounded safe destinations and return path - status: verified
  - [ ] `V030-INTEGRATION-004` Sync schema-1 display-only snapshots with size/count limits - status: implemented
- [x] `V030-LEGACY` Secure legacy XML import - status: verified
  - [x] `V030-LEGACY-001` Parse XML with XXE/DTD disabled into isolated DTOs - status: verified
  - [x] `V030-LEGACY-002` Convert supported fields to canonical definitions and report every unsupported path - status: verified
  - [x] `V030-LEGACY-003` Export deterministic JSON and validate synthetic plus exact-upstream fixtures - status: verified
- [ ] `V030-VERIFY` Version verification and evidence - status: in_progress
  - [ ] `V030-VERIFY-001` Unit, Codec, XML, network, SavedData, and static-identity tests - status: in_progress
  - [ ] `V030-VERIFY-002` GameTest fixed-Level, gravity, command, and invalid-reload matrix - status: planned
  - [ ] `V030-VERIFY-003` Packaged dedicated restart, two-player dimensions, and command evidence - status: planned
  - [ ] `V030-VERIFY-004` Packaged-client Moon/Space visible flow and safe return evidence - status: planned
  - [ ] `V030-VERIFY-005` Release evidence, human Gate review, CI, and merge - status: planned

## Active slice

### `V030-VERIFY-001` Automated verification

- Status: `in_progress`
- Observable outcome: all model, persistence, network, XML, identity, DataGen,
  and client-boundary checks pass from a clean build with durable summaries.
- Scope / non-goals: verify the v0.3.0 slice without weakening limits or
  inheriting human acceptance from an earlier release.
- Dependencies / interfaces: JUnit, repository validators, JAR audit, DataGen,
  Forge GameTest, and deterministic generated-resource manifests.
- Acceptance criteria: every required command exits zero, expected counts and
  hashes are recorded, DataGen leaves no diff, and failures remain visible.
- Verification: clean build, Python/static validators, DataGen, JAR audit, and
  GameTest.
- Evidence: in progress under `docs/releases/v0.3.0/evidence/`.

## Decision log

- `2026-08-31`: accept ADR-001. Only the fixed Moon and Space Levels are data
  registered; catalog reload never changes the loaded Level registry.
- `2026-08-31`: use a project-owned Codec reload listener instead of a custom
  dynamic registry so invalid catalogs can be rejected atomically while fixed
  Level JSON remains vanilla-compatible.
- `2026-08-31`: keep legacy numeric dimension IDs in import diagnostics only.
  Runtime definitions, snapshots, commands, and SavedData use namespaced IDs.
- `2026-08-31`: create a bounded platform during operator `goto` rather than
  force-loading or populating arbitrary Space chunks continuously.
- `2026-08-31`: represent vacuum/gravity as profiles in v0.3; atmosphere damage
  and life-support enforcement remain v0.4 scope.
- `2026-08-31`: keep the legacy parser on bounded StAX DTOs and fail closed when
  DTD/entity support cannot be disabled; no DOM type enters the public adapter.

## Verification log

- `2026-08-31`: v0.2.0 is archived as `PASSED`; branch starts from merge
  commit `11d0ba48b98c3a520948e83988a46a713c9fa08d` with a clean worktree.
- `2026-08-31`: legacy audit identifies the 1,114-line XML loader, 1,164-line
  dimension manager, 2,024-line dimension properties class, and static/digital
  identity risks. None will be copied into the new architecture.
- `2026-08-31`: celestial model suite passes 10/10 focused JUnit tests. It
  covers canonical Codec round trip, invalid numeric/resource fields,
  atmosphere/orbit invariants, duplicate/missing/cyclic parents, deterministic
  ordering, fixed baseline mappings, and the 128-body catalog bound. Plain
  JUnit uses a test-only minimal registry-key bootstrap; real Forge bootstrap
  remains covered by GameTest and packaged-server runs.
- `2026-08-31`: atomic catalog/DataGen suite expands the focused celestial
  result to 15/15. It verifies resource/body identity, per-file payload bounds,
  bounded diagnostics, and last-valid generation retention after rejection.
- `2026-08-31`: `gradlew runData --no-daemon --stacktrace` generated exactly
  the Earth, Moon, and Space catalog files under `src/generated/v0.3`; prior
  v0.1/v0.2 outputs remain read-only existing inputs.
- `2026-08-31`: `gradlew runGameTestServer --no-daemon --stacktrace` passed all
  12 existing GameTests and logged `Accepted celestial catalog generation 1
  with 3 bodies`, proving the listener runs under the real Forge bootstrap.
- `2026-08-31`: the focused celestial suite passes 23/23 after adding bounded
  schema-1 discovery/first-visit NBT, duplicate/type/time validation, exact
  future-schema preservation, rejected mutation, constant-time environment
  lookup, and idempotent Moon/Space gravity-attribute tests.
- `2026-08-31`: the Forge suite now passes 13/13 GameTests, including an
  Overworld-owned SavedData mutation and NBT round trip. Packaged-server restart
  evidence remains required before `V030-STATE-001` is verified.
- `2026-08-31`: DataGen adds fixed Moon and Space dimension/type JSON. A real
  Forge server resolves both `ResourceKey<Level>` and dimension-type keys, then
  saves both worlds on shutdown.
- `2026-08-31`: the Forge suite reaches 15/15 GameTests. A permission-2
  FakePlayer executes `validate`, `list`, `goto moon`, verifies the one-chunk,
  100-write-budget safe platform, and returns with `goto earth`.
- `2026-08-31`: schema-1 display snapshot tests pass for packet envelope and
  payload round trip, strict 128-body/128-character/96-KiB bounds, malformed
  count rejection, and last-valid retention for future schema. The canonical
  three-body payload is below 1 KiB; exact release byte count will be archived
  from the final JAR/test run.
- `2026-08-31`: the import-only adapter passes XXE, depth/input budget, identity,
  path-diagnostic, and exact-upstream fixture tests. The approved fixture is the
  exact 1,031-byte `Template.xml` at upstream commit `c5cd5af6` with SHA-256
  `40674cb8a730e5b6baf2baa3943d34d1712f6f24ffa07db79467627b9c0176e1`.
- `2026-08-31`: two independent Gradle importer runs produced byte-identical
  output (two canonical definitions plus a bounded warning report). The full
  JUnit suite passes 50/50 tests.
