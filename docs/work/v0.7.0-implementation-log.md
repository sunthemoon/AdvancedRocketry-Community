# v0.7.0 Implementation Log — Shared Space stations

## Goal

Deliver persistent, non-overlapping, permissioned stations in the single fixed
Space Level, including player creation, bounded platform generation, rocket
travel, membership, administration, restart recovery, and release evidence.

## Scope and non-goals

In scope: station state/registry, fixed-grid allocation, durable creation
rollback, one approved landing pad, a deployment item, owner/member access,
Space region protection, station-aware flight intents, administrative
inspection/recovery/transfer/deletion, tests, and Gate evidence.

Out of scope: one dimension per station, station movement or resizing, warp
ships, complex orbital mechanics, satellites, terraforming, space elevators,
cross-server stations, and general-purpose claims.

## Assumptions and accepted decisions

- v0.6.0 is `PASSED`; this branch starts from post-merge reproduction commit
  `83bb6748444845aaad9d16f66da8c70f86b737be`.
- ADR-001 keeps one fixed Space Level.
- ADR-006 remains authoritative for durable rocket transfer and recovery.
- Repository owner `sunthemoon` accepted ADR-008 on 2026-09-01.
- New assets and DataGen output are community-authored; no upstream file is
  copied for this version unless provenance is recorded first.

## Feature map

- [x] `V070-PREP` Version boundary and region/ownership ADR — status: accepted
- [ ] `V070-MODEL` Station identity, state, region allocator, access service
- [ ] `V070-PERSIST` Bounded schema codec and Overworld SavedData registry
- [ ] `V070-CREATE` Deployment item, transaction, platform, rollback/recovery
- [ ] `V070-ACCESS` Region protection, membership, removal, operator controls
- [ ] `V070-FLIGHT` Accessible destination sync and approved-pad transfer
- [ ] `V070-CLIENT` Station selection and player-visible environment feedback
- [ ] `V070-VERIFY` Unit/GameTest/dedicated/multiplayer/performance evidence
- [ ] `V070-RELEASE` G0-G9, PR checks, merge, and post-merge reproduction

## Active slice

### `V070-MODEL`–`V070-PERSIST`

- Observable outcome: at least ten deterministic station cells allocate without
  overlap; state, reservations, membership, and schema round-trip exactly.
- Authority: all mutations occur on the server thread and are persisted in
  Overworld SavedData; pure types contain no client dependency.
- Bounds: 4,096 stations, 32 members per station, 512-block logical region,
  1,024-block cell spacing, 17×17 platform, and bounded identifier/NBT sizes.
- Acceptance: duplicate IDs/cells, invalid regions, future schemas, oversized
  lists, unauthorized actions, failed generation, and unsafe deletion fail
  closed without changing another station.

## Decision log

- `2026-09-01`: user confirmation accepts ADR-008 and implementation authority.
- `2026-09-01`: use fixed square-spiral cells rather than free-form placement.
- `2026-09-01`: station UUID is an object identity, not a Level identity.
- `2026-09-01`: retain the v0.6 journaled transfer and add only a server-resolved
  station target; packets remain coordinate-free.

## Verification log

- Pending implementation and executed command evidence.

