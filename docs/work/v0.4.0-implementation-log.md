# v0.4.0 Implementation Log

```yaml
version: v0.4.0
status: IN_PROGRESS
started_at: 2026-08-31
last_updated: 2026-08-31
working_branch: codex/v0.4.0-atmosphere-life-support
base_commit: 93755bc2e89a788d5e1a9bad11fe535e4743333d
tested_implementation_commit: ""
build: 1.20.1-0.4.0-dev
upstream_commit: c5cd5af62fc07cd4e0d24f06a16033f181c47c04
```

## Goal

Deliver server-authoritative vacuum survival, a four-piece space suit with
bounded oxygen storage, one powered Oxygen Vent, and incremental sealed-volume
scanning that cannot force-load chunks or exceed fixed per-tick and total
budgets.

## Scope and non-goals

In scope: `VACUUM`/`BREATHABLE` queries, vacuum damage, suit oxygen, canister
refill, Vent item/energy storage, bounded flood fill, scan deduplication,
volume indexing/invalidation, doors and block-boundary rules, restart-safe
fail-closed behavior, bounded status synchronization, a minimal HUD, debug
commands, DataGen, GameTests, packaged-server restart, two-player evidence,
and a 16-Vent performance run.

Explicit non-goals: no multi-gas simulation, pressure or thermodynamics, toxic
gas, terraforming, arbitrary chunk loading, persistent breathable caches,
unbounded scans, general power network, custom equipment slots, or v0.5+
rocket/station systems.

No upstream source or binary asset will be copied for this slice. The existing
MIT upstream is behavior context only; all v0.4 Java, JSON, text, and any new
visual resources are community-authored and must enter the provenance record.

## Fixed budgets and behavior

| Boundary | Value | Failure behavior |
|---|---:|---|
| Traversable cells per volume | 4,096 | `TOO_LARGE`; stop growth immediately |
| Cell inspections per task per tick | 256 | retain bounded task state |
| Cell inspections per level per tick | 1,024 | round-robin remaining work |
| Active scan tasks per level | 64 | reject new work as `BUSY` |
| Indexed volumes per level | 128 | evict inactive entries fail-closed |
| Indexed traversable cells per level | 65,536 | evict inactive entries fail-closed |
| Dirty positions per level | 8,192 | collapse overflow to bounded cache clear |
| Oxygen per canister | 1,000 units | canister is not consumed unless accepted |
| Suit oxygen capacity | 2,000 units | server rejects overflow |
| Vent oxygen capacity | 4,000 units | server rejects overflow |
| Vent energy capacity/use | 40,000 FE / 20 FE per tick | `NO_POWER` |

An oxygen unit is consumed once per second by a protected player or an active
room provider. A player in a breathable base atmosphere consumes none. Vacuum
damage is evaluated only on the server. A volume is breathable only while its
elected provider has both oxygen and energy; provider loss invalidates
breathability immediately.

Unloaded-neighbor inspection returns `PENDING` without loading the chunk.
Breaking or placing a boundary and opening or closing a door invalidates the
old result before rescanning. Volume cache bytes are intentionally not saved;
after restart or chunk unload, unknown space is vacuum until a fresh bounded
scan succeeds. Vent inventory, oxygen, energy, and schema are persisted by the
BlockEntity.

## Feature map

- [x] `V040-DECISION` Budgets, authority, and recovery policy - status: verified
  - [x] `V040-DECISION-001` Fix scan, index, dirty-queue, energy, and oxygen limits - status: verified
  - [x] `V040-DECISION-002` Fix fail-closed unload/restart and immediate provider-loss behavior - status: verified
- [ ] `V040-SCAN` Pure bounded sealed-volume domain - status: in-progress
  - [x] `V040-SCAN-001` Model cells, boundaries, outcomes, bounds, and diagnostics - status: verified
  - [x] `V040-SCAN-002` Implement resumable budgeted breadth-first scanning - status: verified
  - [ ] `V040-SCAN-003` Implement task collision merge and stable volume identity - status: ready
  - [ ] `V040-SCAN-004` Implement bounded index, invalidation, and eviction - status: planned
- [ ] `V040-LIFE` Pure life-support domain - status: planned
  - [ ] `V040-LIFE-001` Model full-suit, oxygen, environment, and damage transitions - status: planned
  - [ ] `V040-LIFE-002` Enforce finite oxygen capacity and canister transfer - status: planned
- [ ] `V040-FORGE` Server integration and persistence - status: planned
  - [ ] `V040-FORGE-001` Register Oxygen Vent, BlockEntity, armor, and oxygen-canister behavior - status: planned
  - [ ] `V040-FORGE-002` Persist bounded Vent schema and future-schema refusal - status: planned
  - [ ] `V040-FORGE-003` Adapt loaded Level blocks/doors/chunks to the scanner without force load - status: planned
  - [ ] `V040-FORGE-004` Schedule scans, deduplicate providers, index volumes, and process dirty events - status: planned
  - [ ] `V040-FORGE-005` Apply server-authoritative player oxygen and vacuum damage - status: planned
  - [ ] `V040-FORGE-006` Add bounded debug commands and clear player feedback - status: planned
- [ ] `V040-CLIENT` Minimal bounded status display - status: planned
  - [ ] `V040-CLIENT-001` Send versioned finite S2C life-support status only on change/heartbeat - status: planned
  - [ ] `V040-CLIENT-002` Render readable oxygen/environment HUD at supported GUI scales - status: planned
- [ ] `V040-DATA` Authored resources and provenance - status: planned
  - [ ] `V040-DATA-001` Generate models, blockstate, recipes, loot, tags, and localization - status: planned
  - [ ] `V040-DATA-002` Record the exact isolated v0.4 resource inventory and provenance - status: planned
- [ ] `V040-VERIFY` Version verification and evidence - status: planned
  - [ ] `V040-VERIFY-001` Unit-test scan, limits, merge, index, oxygen, and future schema - status: planned
  - [ ] `V040-VERIFY-002` GameTest sealed/open/door/too-large/unloaded/two-room/two-Vent/player matrix - status: planned
  - [ ] `V040-VERIFY-003` Verify restart reconstruction, two-player consistency, and dedicated-server behavior - status: planned
  - [ ] `V040-VERIFY-004` Run 16 Vents for at least five minutes and archive node/tick metrics - status: planned
  - [ ] `V040-VERIFY-005` Capture HUD/player-flow evidence and prepare G0-G9 audit bundle - status: planned
  - [ ] `V040-VERIFY-006` Obtain owner Gate review, pass CI, merge, and reproduce the accepted JAR - status: planned

## Active slice

### `V040-SCAN-003` Scan coordination and stable identity

- Status: `ready`
- Observable outcome: concurrent seeds that discover the same connected space
  converge on one scan/volume identity and one provider set without exceeding
  the shared per-level inspection budget.
- Scope / non-goals: pure task scheduling, collision merge, stable result ID,
  and metrics only; no Level adapter, BlockEntity, or provider consumption.
- Dependencies / interfaces: verified `VolumeScanTask` snapshots and fixed
  task/level limits.
- Acceptance criteria: no tick exceeds 1,024 observations; colliding tasks
  merge without losing seeds; identical cell sets produce identical IDs;
  cancelled/overflow work fails closed.
- Verification: focused coordinator JUnit tests and `gradlew test`.
- Evidence: pending implementation.

## Decision log

- `2026-08-31`: use a project-owned incremental flood fill over loaded cells;
  never use tickets or synchronous chunk loads for atmosphere discovery.
- `2026-08-31`: treat cached volume data as reconstructable acceleration, not
  authoritative persisted state. Restart and unload therefore fail closed.
- `2026-08-31`: elect one provider per sealed volume. Additional Vents attach
  to the same volume but neither multiply breathability nor consume oxygen in
  parallel.
- `2026-08-31`: use vanilla armor slots rather than adding a capability or
  third-party equipment dependency. Oxygen is bounded NBT on the chest piece.
- `2026-08-31`: existing Electrolyzer oxygen canisters become the shared,
  server-validated refill input; no new gas-fluid system is introduced.

## Verification log

- `2026-08-31`: v0.3.0 is accepted and merged at
  `93755bc2e89a788d5e1a9bad11fe535e4743333d`; both local and remote `main`
  passed 3/3 checks and reproduced the accepted JAR twice before this branch.
- `2026-08-31`: preparation audit confirms the current v0.3 environment
  service already exposes constant-time vacuum/breathable profiles. v0.4 can
  extend it without dynamic dimensions, numeric IDs, or client authority.
- `2026-08-31`: pure scan vocabulary and resumable breadth-first task pass 7/7
  focused JUnit tests. Every call enforces the 256-inspection budget; a
  configured 10-cell open space stops at exactly 10 retained cells, unloaded
  data suspends without growth and resumes explicitly, and result collections
  are immutable.
