# v0.8.0 Implementation Log — Progression and data satellites

## Goal

Deliver one complete, server-authoritative research and data-satellite loop:
manufacture components, assemble and launch a logical satellite, let its
mission finish without its owner or chunks being loaded, claim research once,
and spend that research on a persisted celestial discovery.

## Scope and non-goals

In scope: bounded data-driven satellite definitions, logical satellite and
mission state, monotonic game-time scheduling, exact-once research credit,
celestial discovery, one powered Satellite Terminal, bound control chips,
operator recovery, client status, DataGen, dedicated/restart/multiplayer/stress
tests, and release evidence.

Out of scope: satellite entities, full legacy satellite selection, asteroid
mining, terraforming, microwave power, orbital weapons, rocket payload bays,
dynamic dimensions, complete research trees, LibVulpes/ARLib, and copied
upstream assets.

## Assumptions and accepted decisions

- v0.7.0 is `PASSED`; this branch starts from final evidence commit
  `6b33eaf0be1a01b7feceefe785d8fba7e9717b02`.
- ADR-010 is the accepted SavedData, clock, scheduler, claim, and terminal
  authority boundary.
- Existing machine casing, advanced circuit, and data storage recipes form the
  minimum manufacturing prerequisite; no general machine framework is added.
- New Java and generated JSON are community-authored. The locked upstream
  commit supplies behavior context only unless a provenance entry is approved
  before any import.

## Feature map

- [x] `V080-PREP` Version boundary, ADR, branch, and zero-copy baseline — status: verified
- [x] `V080-DEFINITION` Bounded data-driven satellite catalog — status: verified
- [x] `V080-MODEL` Satellite, mission, research, clock, and scheduler domain — status: verified
- [x] `V080-PERSIST` Independent fail-closed NBT schemas and Overworld SavedData — status: verified
- [x] `V080-TERMINAL` Powered assembly, bound chip, idempotent launch, receive — status: verified
- [x] `V080-DISCOVERY` Exact-once research claim and celestial discovery — status: verified
- [x] `V080-CLIENT` Server-validated menu and player-readable status — status: verified
- [x] `V080-ADMIN` Inspect, recover, cancel, and bounded diagnostics — status: verified
- [x] `V080-DATAGEN` Definitions, recipes, models, blockstate, loot, tags, language — status: verified
- [x] `V080-VERIFY` Unit, GameTest, dedicated, restart, multiplayer, security, stress — status: verified
- [ ] `V080-RELEASE` G0-G9 evidence, PR, merge, and post-merge reproduction — status: in-progress

## Active slice

### `V080-PREP-01` Fix the version and design boundary

- Status: verified
- Observable outcome: repository status identifies v0.8.0, its isolated branch,
  build version, accepted persistence design, and explicit non-goals.
- Scope / non-goals: documentation and build identity only; no gameplay code.
- Dependencies / interfaces: v0.7.0 immutable Gate record, ADR-010,
  `gradle.properties`, current-version and Gate documents.
- Acceptance criteria: branch starts at final v0.7.0 `main`; ADR fixes bounds,
  no-chunk scheduling, exact-once claim, and authority; base recipes are present;
  repository validator and `git diff --check` pass.
- Verification: `python scripts/validate_repository.py --require-approved-identity`
  and `git diff --check`.
- Evidence: strict repository validation passed 38 checks with zero warnings or
  failures; `git diff --check` passed on 2026-09-01.

### `V080-DEFINITION-01` Decode one complete bounded definition catalog

- Status: verified
- Observable outcome: the server accepts an exact `data_satellite` definition
  and retains the previous catalog when a reload is invalid.
- Scope / non-goals: definition/model/catalog only; no runtime mission state.
- Dependencies / interfaces: ADR-010, Forge JSON reload listener, celestial
  catalog target validation.
- Acceptance criteria: strict IDs, target count, duration, yield, duplicates,
  absent type, oversized JSON, and atomic reload behavior are tested.
- Verification: focused JUnit catalog/codec tests.
- Evidence: production reload registration, exact generated definition, and
  codec/catalog/reload-manager tests pass in the 247-test JUnit suite.

### `V080-MODEL-01` Prove bounded mission lifecycle and scheduler work

- Status: verified
- Observable outcome: 100 logical missions finish in deadline order without a
  per-tick collection scan or any Minecraft world/chunk reference.
- Scope / non-goals: pure Java model, clock, registry, and priority queue only.
- Dependencies / interfaces: accepted definitions and fixed limits.
- Acceptance criteria: legal start/ready/claim/cancel transitions, monotonic
  rollback behavior, exact-once claim, ownership, capacity, and per-pass budget.
- Verification: focused JUnit model and scheduler tests.
- Evidence: focused scheduler/model tests and the complete 247-test JUnit suite
  pass. The 100-mission packaged run completes in bounded passes of 32, 32, 32,
  and 4 with no chunk tickets.

### `V080-TERMINAL-01` Complete the player progression loop

- Status: verified
- Observable outcome: a powered terminal consumes the bounded component set,
  creates an owner-bound satellite package, launches once, exposes mission
  state, claims research once, and records a celestial discovery.
- Scope / non-goals: one logical data satellite and one combined terminal; no
  orbiting entity, payload bay, or general machine framework.
- Dependencies / interfaces: v0.2 energy capability, satellite definitions,
  Overworld SavedData, server menu handlers, and generated recipes/assets.
- Acceptance criteria: insufficient power/components/target fail readably;
  valid launch is idempotent; claim replay cannot duplicate research.
- Verification: JUnit, Forge GameTest, ordered terminal screenshots, and the
  packaged satellite ledger.
- Evidence: 247/247 JUnit, 44/44 GameTests, exact-once packaged claims, and the
  seven-frame pre-candidate visual sequence all pass.

### `V080-AUTHORITY-01` Bound client, operator, and persistence authority

- Status: verified
- Observable outcome: clients send bounded intents only; the server validates
  player, distance, ownership, power, inventory, state, range, and loaded
  terminal before mutation. Permission-level-two recovery is auditable.
- Scope / non-goals: terminal and satellite operations only; no generic remote
  administration protocol.
- Dependencies / interfaces: menu listeners, SavedData schemas, bounded
  commands, and celestial discovery service.
- Acceptance criteria: non-owner and replay attempts change nothing; future
  schemas preserve payload and fail closed; no client supplies final mission
  values or coordinates.
- Verification: authority matrix, GameTests, JUnit schema tests, and client
  import boundary scan.
- Evidence: ten authority cases pass with zero Critical/High findings and zero
  client-side final satellite decisions.

### `V080-VERIFY-01` Exercise packaged, restart, multiplayer, and stress flows

- Status: verified
- Observable outcome: the exact candidate survives first start, save, stop,
  same-world restart, two-owner missions, two simultaneous clients, another
  server restart, and both clients reconnecting to unchanged shared state.
- Scope / non-goals: loopback Forge user-development clients and a packaged
  Forge server; no cross-platform byte-equality claim.
- Dependencies / interfaces: candidate JAR, Java 17, Forge 47.4.10, release
  hooks, multiplayer smoke runner, and JFR.
- Acceptance criteria: 44 GameTests, 100 ready stress missions, scheduler work
  within 32/64 limits, two distinct clients in both cycles, zero permanent
  chunk tickets, and tick mean/P95/max below 50 ms.
- Verification: candidate artifact audit, packaged scripts, filtered logs,
  screenshots, and JFR summary.
- Evidence: SHA-256 `0ce6c6bf...a1937`, 723 JAR entries, two byte-identical
  clean builds, 44/44 GameTests, two two-client cycles, and a 13-sample
  workload window with 6.536 ms mean and 22.831 ms P95.

### `V080-RELEASE-01` Bind the release candidate and complete merge audit

- Status: in-progress
- Observable outcome: G0-G9 are evidence-bound, owner-approved, reviewed in one
  PR, and the exact merge commit rebuilds the same JAR and content manifest.
- Scope / non-goals: internal `-dev` milestone only; no tag or public release.
- Dependencies / interfaces: release bundle, checksums, workflows, PR checks,
  merge identity, and post-merge reproduction record.
- Acceptance criteria: strict repository validation has zero warnings/failures;
  required PR checks pass; post-merge clean build is byte-identical; status then
  changes from `READY_FOR_AUDIT` to `PASSED`.
- Verification: `validate_v080_release_evidence.py`, strict repository validator,
  GitHub required checks, and a forced cache-disabled clean merge build.
- Evidence: candidate bundle and owner G0/G8/G9 approval are complete; PR,
  required checks, merge, and post-merge reproduction remain.

## Decision log

- `2026-09-01`: use one logical data-satellite type and a single combined
  terminal rather than restoring legacy multiblocks or orbiting entities.
- `2026-09-01`: player offline means the server continues advancing game time;
  a stopped server does not synthesize wall-clock progress.
- `2026-09-01`: research credit and mission claim share one SavedData mutation;
  celestial discovery is replayed idempotently through an explicit phase.
- `2026-09-01`: assembly/launch control uses a UUID-bound chip; clients never
  supply a coordinate, research amount, duration, yield, or authoritative NBT.
- `2026-09-03`: retain the two immutable v0.1 block-tag source files for their
  historical provenance audit, while `processResources` and `sourcesJar`
  precisely exclude those old copies so the v0.8 aggregate tags are the only
  packaged entries. Global duplicate handling remains strict.

## Verification log

- `2026-09-01`: v0.8.0 branch and build identity established; ADR-010 and both
  prerequisite checks recorded. Strict repository validation passed 38 checks
  with zero warnings or failures, and `git diff --check` passed.
- `2026-09-01`: data definition, logical satellite, mission phases, research
  account, monotonic rollback clock, and bounded deadline queue compiled. All
  236 JUnit tests passed; the first dependency-resolution attempt failed on a
  transient MCP connection reset and the retry reached and passed compilation.
- `2026-09-01`: Overworld-owned satellite mission SavedData, strict record
  codecs, idempotent launch, restart deadline recovery, owner checks, and
  exact-once research/discovery claim phases passed the complete 244-test JUnit
  suite. Root, satellite, mission, and research-account future schemas preserve
  their original payload and block all mutation.
- `2026-09-03`: final candidate JAR is 1,166,061 bytes with 723 entries and
  SHA-256 `0ce6c6bf9eb603f5973f35c19a47b295454a1f8c74ee74a6a99af3c2627a1937`;
  a second cache-disabled clean build reproduced it byte-for-byte.
- `2026-09-03`: 247/247 JUnit and 44/44 Forge GameTests pass. Packaged first
  start/restart, two-owner exact-once claims, 100-mission stress, simultaneous
  two-client join/disconnect, server restart, and two-client reconnect pass.
- `2026-09-03`: the 100-mission JFR workload measured 6.536 ms mean, 22.831 ms
  P95, 37.667 ms maximum, and 0.0243% GC overhead with zero chunk tickets.
- `2026-09-03`: owner `sunthemoon` approved G0, G8, and G9. ADR-011 records the
  v0.8.0-only ordered-screenshot evidence decision. Candidate state is
  `READY_FOR_AUDIT` pending PR checks, merge, and exact post-merge reproduction.
- `2026-09-03`: restoring the audited v0.1 block-tag files initially exposed
  duplicate `processResources` and `sourcesJar` inputs. Both tasks now exclude
  only the historical copies; a forced cache-disabled clean build passes and
  still produces the exact 1,166,061-byte, 723-entry candidate JAR.
