# v0.9.0 Implementation Log — Beta hardening

## Goal

Freeze the accepted v0.8.0 feature set and produce a Beta candidate that can
upgrade an accepted Alpha world safely, run the complete core loop on a small
dedicated server for two hours, remain inside fixed performance/security
bounds, and publish an auditable compatibility and support contract.

## Scope and non-goals

In scope: pre-start SavedData backup and sequential migration, failure rollback,
migration fixtures, bounded configuration, stable diagnostics, packet-abuse and
authority regression, maximum combined load, two-hour soak, Forge baseline and
latest lanes, JEI present/absent, accessibility/resource audit, and release
evidence.

Out of scope: new content systems, new destinations or satellite types, direct
1.12.2 world conversion, broad architecture replacement, multi-loader support,
and universal modpack compatibility.

## Assumptions and open decisions

- v0.8.0 is `PASSED` at final evidence commit
  `0d59c01da458e13ed0014e98f91379c6f783e19d`; all four final checks passed.
- `1.20.1-0.9.0-beta.1` is the first candidate identity.
- The repository owner approved ADR-012's feature-freeze and support boundary
  on 2026-09-03. This scope approval does not replace technical Gate evidence.
- The supported valued-world source is v0.8.0. v0.5–v0.7 remain migration
  fixtures and codec regressions, not a blanket direct-world promise.
- No upstream source or asset import is planned. Any later import must enter
  provenance before it enters production paths.

## Feature map

- [x] `V090-PREP` Freeze, support contract, branch, and build identity — status: verified
- [x] `V090-MIGRATE` Managed SavedData backup, migration, rollback, and fixtures — status: verified
- [x] `V090-DIAG` Bounded config, stable diagnostic IDs, and operator report — status: verified
- [x] `V090-NET` Malicious packet corpus, rate, authority, and side checks — status: verified
- [ ] `V090-COMPAT` Forge baseline/latest and JEI present/absent matrix — status: in-progress
- [ ] `V090-STRESS` Maximum combined scenario and two-hour four-client soak — status: in-progress
- [ ] `V090-RECOVERY` Alpha backup-to-Beta continuation and forced-stop recovery — status: in-progress
- [ ] `V090-UX` GUI scale, localization, accessibility, and resource integrity — status: ready
- [ ] `V090-AUDIT` Critical/High defect, duplication, permission, and ticket audit — status: ready
- [ ] `V090-RELEASE` Candidate evidence, checksums, PR, merge, and reproduction — status: ready

## Active slice

### `V090-PREP-01` Bind the Beta branch and prerequisite

- Status: verified
- Observable outcome: status documents identify v0.9.0, the isolated branch,
  accepted v0.8.0 prerequisite, and exact proposed build.
- Scope / non-goals: metadata only; no compatibility claim.
- Dependencies / interfaces: v0.8.0 immutable Gate record and final `main` CI.
- Acceptance criteria: branch starts at final v0.8.0 evidence; worktree is
  clean; all four prerequisite checks are successful.
- Verification: Git history/status and GitHub check-run API.
- Evidence: branch `codex/v0.9.0-beta-hardening` starts at `0d59c01...`; final
  v0.8.0 `main` reports 4/4 successful checks on 2026-09-03.

### `V090-PREP-02` Define the feature freeze

- Status: verified
- Observable outcome: allowed hardening work and prohibited content expansion
  are explicit and reviewable.
- Scope / non-goals: scope decision only; does not approve implementation.
- Dependencies / interfaces: v0.9.0 plan and ADR-012.
- Acceptance criteria: every planned capability fits the allowed list; an
  exception requires an accepted ADR; repository owner records approval.
- Verification: document review and scope audit of changed production files.
- Evidence: `docs/work/v0.9.0-feature-freeze.md`; repository-owner approval
  recorded 2026-09-03.

### `V090-PREP-03` Define the Beta support contract

- Status: verified
- Observable outcome: players and operators can identify supported Java,
  Minecraft, Forge, save upgrade, optional-mod, server-scale, and report scope.
- Scope / non-goals: no stable or universal compatibility promise.
- Dependencies / interfaces: project compatibility policy and ADR-012.
- Acceptance criteria: support policy is internally consistent with the release
  plan and is linked from durable documentation; owner approves ADR-012.
- Verification: relative-link validation and owner review.
- Evidence: `docs/BETA-SUPPORT-POLICY.md`; repository-owner approval recorded
  2026-09-03.

### `V090-PREP-04` Establish the Beta build and CI baseline

- Status: verified
- Observable outcome: local/CI artifacts use `1.20.1-0.9.0-beta.1` and every
  historical validator remains active.
- Scope / non-goals: identity and automation only; no Gate is passed by rename.
- Dependencies / interfaces: Gradle properties, metadata, workflows, artifact
  audit, packaged smoke commands.
- Acceptance criteria: clean build passes with Java 17; JAR identity and
  metadata are exact; strict repository validation passes.
- Verification: Gradle build, artifact audit, metadata tests, strict validator.
- Evidence: Java 17 clean build passed 247/247 tests; the 723-entry initial JAR
  is named `advancedrocketry-community-1.20.1-0.9.0-beta.1.jar`, carries the
  exact processed metadata, and passed the artifact audit with SHA-256
  `351bc0fb5c559052e4631ea96772bef3ce7f91a4469841267e7d12c18c5c0f1a`.
  Workflow-structure tests passed 33/33 and strict repository validation passed
  41 checks with zero pending items, warnings, or failures.

### `V090-MIGRATE-01` Define the sequential root-schema migration

- Status: verified
- Observable outcome: v0.5–v0.8 managed SavedData schema 1 decodes into a
  canonical schema 2 payload without changing nested authority data.
- Scope / non-goals: five Overworld SavedData roots; chunk entity and
  BlockEntity schemas keep their existing independently tested migration paths.
- Dependencies / interfaces: celestial, rocket transaction/transfer, station,
  and satellite SavedData codecs.
- Acceptance criteria: deterministic v1→v2 transforms; current data is
  idempotent; future/missing/malformed/oversized data fails closed; successful
  in-memory migration marks SavedData dirty.
- Verification: fixture-driven JUnit and existing persistence regression tests.
- Evidence: five fixed managed roots migrate schema 1 to schema 2 through
  `SavedDataSchemaMigrator`; fixture, loader, malformed/future, idempotence,
  defensive-copy, and existing persistence suites pass.

### `V090-MIGRATE-02` Back up and atomically replace managed world files

- Status: verified
- Observable outcome: server pre-start discovers a needed migration, backs up
  every managed source, stages and rereads all outputs, then commits them as one
  recoverable transaction.
- Scope / non-goals: fixed ARCE files under the world's `data` directory; not a
  replacement for an operator's full-world backup.
- Dependencies / interfaces: bounded compressed-NBT reader, world path, UTC
  clock, fixed backup limit, SHA-256 manifest, same-directory atomic moves.
- Acceptance criteria: no change without an upgrade; backup predates writes;
  all files migrate or originals are restored byte-for-byte; symlinks and path
  escapes are rejected; backups survive success/failure.
- Verification: temporary-world tests, injected mid-commit failure, and
  packaged v0.8→v0.9 server restart smoke.
- Evidence: `WorldDataMigrationServiceTest` proves no-op behavior, byte-exact
  all-source backup, staged reread, all-fixture migration, injected second-file
  commit rollback, five-backup limit, compressed/expanded byte limits, and
  symbolic-file rejection. The packaged server migrated all five roots and
  restarted the same world with every subsystem operational.

### `V090-MIGRATE-03` Archive executable Alpha fixtures

- Status: verified
- Observable outcome: representative v0.5, v0.6, v0.7, and v0.8 root payloads
  remain immutable inputs to the Beta migration tests.
- Scope / non-goals: fixture-level historical formats, not a direct support
  promise for every pre-v0.8 world.
- Dependencies / interfaces: test resources and subsystem codecs.
- Acceptance criteria: fixture hashes are inventoried; every fixture migrates,
  round-trips current schema, and retains its authority collections.
- Verification: fixture manifest validator and JUnit migration suite.
- Evidence: five UTF-8 SNBT fixtures and canonical
  `src/test/resources/migrations/v090/manifest.json`; Python tamper/inventory
  tests and both blocking workflows enforce exact sizes and SHA-256 values.

### `V090-MIGRATE-04` Expose startup diagnostics and recovery instructions

- Status: verified
- Observable outcome: operators receive one stable ID for no-op/current,
  successful migration, blocked schema, backup-limit, commit rollback, or fatal
  rollback failure.
- Scope / non-goals: bounded startup reporting; no debug log spam.
- Dependencies / interfaces: Forge server lifecycle and migration report.
- Acceptance criteria: migration failure prevents normal startup, preserves
  originals, names the backup/recovery action, and emits no machine path into
  committed evidence.
- Verification: unit diagnostics assertions and packaged lifecycle log filter.
- Evidence: stable `ARCE-BETA-1000`–`ARCE-BETA-2008` identifiers, pre-start
  lifecycle logging, startup abort on failure, and permission-level-2
  `arce beta data-report` are implemented. Packaged filtered logs contain the
  successful migration, current-schema restart, and two operational reports.

### `V090-NET-01` Reject malformed flight-intent frames

- Status: verified
- Observable outcome: the only Beta C2S packet accepts exactly one bounded,
  canonical frame and rejects truncated, oversized, overlong, unknown, or
  trailing-byte inputs before service execution.
- Scope / non-goals: `RocketFlightIntentPacket` framing only; no protocol
  feature or compatibility-policy change.
- Dependencies / interfaces: Forge `FriendlyByteBuf`, protocol version 2, and
  the existing server-authoritative flight service.
- Acceptance criteria: an explicit encoded-size ceiling; no unread bytes after
  decode; a fixed malicious corpus covers every variable boundary and invalid
  identifier without leaking buffers.
- Verification: focused JUnit packet suite followed by the full unit suite.
- Evidence: the decoder enforces a 39-byte ceiling, canonical VarInts, complete
  consumption, and valid destination identity. The focused malicious-corpus
  suite and the 272-test clean build pass without retained Netty buffers.

### `V090-NET-02` Re-audit C2S authority and request limits

- Status: verified
- Observable outcome: distance, permission, loaded-chunk, state, replay, and
  rate-limit denials remain side-effect free and no common/server class imports
  client code.
- Scope / non-goals: regression proof and defect fixes only; no new gameplay.
- Dependencies / interfaces: `RocketFlightService`, request ledger, station
  access, and the common/client import scanner.
- Acceptance criteria: hostile intent cases pass; the rate limiter stays
  bounded; client-import scan passes; no request can force-load a chunk.
- Verification: focused JUnit/GameTest suites and `check_client_imports.py`.
- Evidence: focused authority/rate tests and the common/client import scan pass.
  Clean baseline Forge 47.4.10 and latest-lane Forge 47.4.23 runs each passed
  all 44 required GameTests after the hardening changes.

### `V090-DIAG-01` Audit bounded server configuration

- Status: verified
- Observable outcome: every exposed Beta common setting has a documented safe
  default and enforced minimum/maximum; default operation emits no debug spam.
- Scope / non-goals: existing performance controls only; no dynamic sync layer
  for settings that are already server authoritative.
- Dependencies / interfaces: Forge common config and atmosphere budgets.
- Acceptance criteria: config specs are unit-tested at both bounds; obsolete
  pre-Alpha wording or unused toggles are removed or made truthful.
- Verification: configuration unit tests and generated/default config review.
- Evidence: the common spec exposes exactly the two active atmosphere limits;
  reflection-based tests prove defaults and both bounds in the 272-test build.

### `V090-DIAG-02` Publish one bounded operator report

- Status: verified
- Observable outcome: permission-level-2 `arce beta report` prints one stable,
  path-free summary of build, Forge, protocol, config, persistence, station,
  mission, transfer, and chunk-ticket state.
- Scope / non-goals: aggregate support data only; no player UUIDs, paths, or
  unbounded object dumps.
- Dependencies / interfaces: existing Beta data report and subsystem counters.
- Acceptance criteria: stable diagnostic ID, fixed fields and length, clear
  degraded-state markers, translated player-facing denial/error messages.
- Verification: unit contract test plus packaged-server log assertion.
- Evidence: `ARCE-BETA-1101` is fixed at no more than 1,024 path-free characters and
  has contract tests for operational/degraded roots and unsafe version input.
  The exact packaged JAR emitted a schema-2 operational report before and after
  migration restart.

### `V090-COMPAT-01` Isolate and pin optional JEI support

- Status: in-progress
- Observable outcome: JEI can display the Electrolyzer recipe when the exact
  supported JEI build is installed, while ARCE builds and starts without JEI.
- Scope / non-goals: one existing recipe category; JEI remains optional and
  client-only, with no JEI types crossing into common/server packages.
- Dependencies / interfaces: official JEI 1.20.1 Forge API and Electrolyzer
  recipe/item registrations.
- Acceptance criteria: compile-only API, runtime opt-in dependency, optional
  metadata, isolated plugin, and automated present/absent initialization proof.
- Verification: Gradle compile/test, absent packaged server, present client
  startup log, and client-import scan.
- Evidence: exact JEI 15.56.0.205 compile-only APIs, opt-in runtime, optional
  metadata, isolated client plugin/category, provenance hashes, and absent
  packaged-server initialization pass. A preliminary four-cell client run
  joined the same packaged server both with and without JEI on Forge 47.4.10
  and 47.4.23; it exposed a vanilla recipe-book category warning. The machine
  recipe now explicitly reports `isSpecial()`, with JUnit and GameTest coverage;
  the final exact-JAR matrix rerun remains.

### `V090-COMPAT-02` Execute the supported compatibility matrix

- Status: in-progress
- Observable outcome: Forge 47.4.10 and 47.4.23 results, each with JEI absent
  and present where client-side applicable, are archived against one JAR hash.
- Scope / non-goals: the published Beta support matrix only.
- Dependencies / interfaces: pinned Forge/JEI properties and CI jobs.
- Acceptance criteria: baseline is blocking; latest result is recorded; all
  four matrix cells have command, version, result, and evidence.
- Verification: CI plus local packaged/client compatibility harnesses.
- Evidence: clean builds and all 44 required GameTests pass on both Forge
  47.4.10 and 47.4.23. The four final client cells remain to be archived against
  the frozen candidate JAR.

### `V090-STRESS-01` Build the maximum combined Beta scenario

- Status: implemented-unverified
- Observable outcome: one bounded test world concurrently contains the maximum
  rocket snapshot, 16 vents, 10 stations, and 100 queued missions.
- Scope / non-goals: release-test hooks enabled only by an explicit JVM system
  property; no production shortcut or relaxed limit.
- Dependencies / interfaces: existing maximum GameTests and packaged station,
  satellite, atmosphere, and rocket harnesses.
- Acceptance criteria: exact counts are asserted; budgets and zero permanent
  tickets are reported; save/restart preserves authority and material counts.
- Verification: packaged combined-scenario harness and evidence validator.
- Evidence: `run_v090_soak_server.py` assembles and verifies the exact combined
  counts, refuses durations below 7,200 real seconds, samples tick/CPU/RSS/GC,
  and validates the same world after restart. Runtime execution remains.

### `V090-STRESS-02` Run a two-hour four-client simulation soak

- Status: implemented-unverified
- Observable outcome: the combined world runs continuously for at least 7,200
  seconds while four independent status clients probe it and periodic saves and
  bounded gameplay operations continue.
- Scope / non-goals: protocol-level multi-client simulation, explicitly not a
  substitute for the already approved visual/manual client flow.
- Dependencies / interfaces: `V090-STRESS-01`, Java/JVM metrics, server query,
  and exact candidate JAR.
- Acceptance criteria: no watchdog/crash/project ERROR; no sustained heap or
  ticket growth; mean/P95/max tick, CPU, RSS, GC, saves, client probes, and
  operation counts are archived.
- Verification: wall-clock harness result and independently validated summary.
- Evidence: the harness schedules four concurrent status-protocol clients every
  15 seconds, periodic saves/refills/reports, fixed tick and sustained-memory
  budgets, live checkpoints, and final log/ticket audit. Runtime execution
  remains.

### `V090-RECOVERY-01` Continue play after Alpha-to-Beta migration

- Status: in-progress
- Observable outcome: a backed-up v0.8 world migrates once, then completes
  machine, atmosphere, rocket, station, and satellite operations before and
  after restart.
- Scope / non-goals: the valued v0.8 source contract only.
- Dependencies / interfaces: migration transaction and packaged core harnesses.
- Acceptance criteria: source backup is byte-exact; all root schemas are 2;
  post-migration writes persist; a second start is idempotent.
- Verification: packaged continuation/restart harness.
- Evidence: candidate JAR `caef065c...9195` migrated all five schema-1 roots,
  retained the byte-exact backup, emitted the bounded operator report, and
  restarted idempotently. Core operations on that migrated world remain.

### `V090-RECOVERY-02` Prove forced-stop recovery

- Status: ready
- Observable outcome: a test-world server killed at an armed durable checkpoint
  restarts without duplication, loss, or schema rollback.
- Scope / non-goals: only the process launched by the harness may be killed;
  release-test hooks and a disposable copied world are mandatory.
- Dependencies / interfaces: transaction journals, save command, process tree,
  and exact ledger counts.
- Acceptance criteria: pre-kill checkpoint is observed; non-graceful exit is
  recorded; restart recovery reaches one legal outcome with exact invariants.
- Verification: packaged forced-stop harness and filtered lifecycle evidence.
- Evidence: pending.

### `V090-UX-01` Audit localization, diagnostics, and resources

- Status: ready
- Observable outcome: en_us/zh_cn cover every referenced player-facing key;
  assets resolve case-exactly; diagnostic IDs and failure text are readable.
- Scope / non-goals: correction and accessibility only; no visual redesign.
- Dependencies / interfaces: DataGen language providers, generated assets, and
  existing screens/tooltips.
- Acceptance criteria: no missing/extra required key, missing texture/model, or
  unlocalized error; color is not the sole status signal.
- Verification: deterministic DataGen and dedicated resource/localization audit.
- Evidence: pending.

### `V090-UX-02` Archive approved GUI-scale/manual evidence

- Status: ready
- Observable outcome: the Beta manual record identifies the tested JAR, date,
  tester, GUI scales, flows, and the repository owner's G8/G9 decision.
- Scope / non-goals: no fabricated screenshot; prior approved gameplay evidence
  may be referenced when the exact UI/content is unchanged.
- Dependencies / interfaces: immutable prior release screenshots and current
  build/resource audit.
- Acceptance criteria: every required field is present and provenance points to
  real files; approval does not replace automated technical checks.
- Verification: manual-evidence validator and owner confirmation record.
- Evidence: owner authorized direct G8/G9 confirmation on 2026-09-03; record
  assembly remains pending.

### `V090-AUDIT-01` Complete the Beta security and defect audit

- Status: ready
- Observable outcome: Critical/High defects, duplication/loss, permissions,
  packet authority, chunk loading, caches, and ticket ownership have explicit
  findings and regression evidence.
- Scope / non-goals: frozen v1.0 core systems only.
- Dependencies / interfaces: network, maximum scenario, recovery, provenance,
  and repository validators.
- Acceptance criteria: zero open Critical/High; exact before/after ledgers; zero
  permanent ARCE tickets; all findings link to evidence or a fixed regression.
- Verification: audit checklist, static scans, unit/GameTest, packaged results.
- Evidence: pending.

### `V090-RELEASE-01` Assemble and validate the Beta candidate record

- Status: ready
- Observable outcome: test report, performance, compatibility, migration,
  security, manual, known-issues, release evidence, and checksums agree on one
  commit and JAR SHA-256.
- Scope / non-goals: Beta pre-release only, never a stable claim.
- Dependencies / interfaces: every preceding verified capability.
- Acceptance criteria: strict repository and v0.9 evidence validators pass with
  no pending Required Gate; version status is `READY_FOR_AUDIT` until approval.
- Verification: validators, link audit, and GitHub checks.
- Evidence: pending.

### `V090-RELEASE-02` Merge and reproduce the approved candidate

- Status: ready
- Observable outcome: PR #13 is reviewed, green, approved, merged; clean
  `main` rebuilds byte-for-byte the same distributable JAR and the merge CI is
  green before the annotated Beta pre-release/tag is published.
- Scope / non-goals: no post-candidate source change hidden in evidence.
- Dependencies / interfaces: `V090-RELEASE-01`, repository-owner approval, PR
  checks, protected-branch policy, and deterministic build.
- Acceptance criteria: merged tree equals reviewed tree except the merge
  topology; SHA-256 matches before/after; release is correctly marked Beta.
- Verification: GitHub API, clean clone/worktree build, hash comparison, tag and
  release API readback.
- Evidence: pending.

## Decision log

- `2026-09-03`: infer the requested `v0.l9.0` as repository milestone v0.9.0.
- `2026-09-03`: keep v0.9.0 feature-frozen and use v0.8.0 as the valued-world
  upgrade floor; earlier schemas remain explicit fixtures.
- `2026-09-03`: propose a staged world-level migration transaction rather than
  relying on independent SavedData load order.
- `2026-09-03`: repository owner approved the feature freeze and Beta support
  boundary; technical and release Gates remain evidence-driven.

## Verification log

- `2026-09-03`: final v0.8.0 main commit
  `0d59c01da458e13ed0014e98f91379c6f783e19d` is clean and synchronized with
  `origin/main`; Forge baseline, v0.8 satellite Gate, Forge latest advisory,
  and governance checks all completed successfully.
- `2026-09-03`: Beta build identity and every packaged regression command were
  advanced to `1.20.1-0.9.0-beta.1`. Clean build passed 247/247 JUnit tests;
  artifact audit passed 723 entries; 33 workflow contract tests and all 41
  strict repository checks passed.
- `2026-09-03`: migration unit and temporary-world suites pass inside a clean
  261-test Beta build. Five fixtures are hash-bound; the world transaction
  rejects unsafe/future/malformed/oversized inputs and restores every original
  after an injected second-file commit failure. The strict validator reported
  42 passes before the packaged migration harness was added; its workflow and
  harness contract tests subsequently passed 40/40.
- `2026-09-03`: packaged Forge server commit `b5483a7...` and JAR
  `sha256:468c254d3c7c96cac2c98628aa000916ae84cbbaf834ff43c69d26174415bfe3`
  migrated all five schema-1 fixtures after a byte-exact backup, then restarted
  as current schema with every subsystem operational. Rebuilding after the
  test-harness-only commit reproduced the exact same JAR hash.
- `2026-09-03`: clean build at `805adb3...` passed 272/272 JUnit tests, the
  758-entry artifact audit, and the common/client boundary scan. The packaged
  candidate JAR is `sha256:caef065c1b1906fba6e037c7f2e029727a4586189084212b461191250c699195`;
  first start, status, save, stop, and same-world restart all passed.
- `2026-09-03`: the updated packaged migration harness verified five-root
  schema-1-to-2 migration, byte-exact backup, `ARCE-BETA-1101`, and idempotent
  same-world restart against the same candidate JAR.
- `2026-09-03`: clean baseline Forge 47.4.10 and latest-lane Forge 47.4.23
  builds each passed 273/273 JUnit tests and all 44 required GameTests. A real
  four-cell JEI present/absent client run found that the custom machine recipe
  was entering the vanilla recipe book; the Beta fix marks it special and adds
  unit plus GameTest regression assertions before the final matrix rerun.
